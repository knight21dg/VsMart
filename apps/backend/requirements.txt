# Core
Django==5.0.*
djangorestframework==3.15.*
djangorestframework-simplejwt==5.3.*
django-environ==0.11.*          # .env loading
psycopg[binary]==3.1.*          # PostgreSQL driver
gunicorn==22.0.*

# Async / jobs
celery==5.4.*
redis==5.0.*
django-redis==5.4.*             # cache backend

# Real-time (WebSockets) — live dispatch board + customer order tracking
channels[daphne]==4.*           # ASGI + WebSocket consumers (daphne = ASGI server)
channels-redis==4.*             # Redis channel layer (prod); dev uses in-memory

# Push notifications — FCM HTTP v1 (the legacy fcm/send API was shut down 2024-06).
firebase-admin==6.*             # service-account auth + multicast send; no-op without creds

# Storage / files
django-storages==1.14.*         # S3/MinIO backend
boto3==1.34.*
Pillow==10.*                    # image handling

# API niceties
django-cors-headers==4.4.*
django-filter==24.*             # list filtering
djangorestframework-camel-case==1.4.*   # camelCase to match the Flutter app
drf-spectacular==0.27.*         # OpenAPI schema/docs

# Payments / external
razorpay==1.4.*
setuptools>=70,<81              # razorpay 1.4 imports pkg_resources — REMOVED in setuptools 81; keep the legacy provider
requests==2.32.*

# Dev / quality (optional, move to requirements-dev.txt later)
pytest==8.*
pytest-django==4.8.*
ruff==0.6.*
factory-boy==3.3.*

# Reports export
openpyxl==3.1.*
reportlab==4.*

# Error monitoring (optional; activated in prod when SENTRY_DSN is set)
sentry-sdk==2.*
razorpay==1.4.*
